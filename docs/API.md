# API と、クライアント–サーバの分け方

## なぜこの形なのか

このアプリは **HTML ファイル 1 枚でオフラインで動く**ことを手放したくない。
一方で、社内サーバやクラウドに置いて複数人で使えるようにもしたい。
両立させるために、**画面は API しか知らない**構造にしてある。

```
        画面 (TypeScript)
             │
             ▼
        ApiClient           ← 画面が知っているのはこの口だけ
        ╱        ╲
LocalApiClient   HttpApiClient
   │                 │
   ▼                 ▼
 WASM              HTTP
   │                 │
   ╰────────┬────────╯
            ▼
      crates/api の Service       ← 権限も不変条件もここにしかない
            │
            ▼
         Store (差し替え可能)
```

肝心なのは、**どちらの経路でも最後は同じ Rust の実装に行き着く**ことだ。
権限の判定を画面側や HTTP 層に散らすと、「画面では隠していたのにサーバでは
通ってしまう」という食い違いが必ず生まれる。ここでは `crates/api` の
`Service` を通らずにデータへ触る道が無い。

ローカルだから素通し、ということもない。`LocalApiClient` でも
「閲覧者は保存できない」「最後の所有者は外せない」といった判定が同じように働く。

### この形の代償

API 層を Rust に置いて JSON でやり取りするため、WASM に serde_json が入り、
75 KiB だったバイナリが 322 KiB になった (単一 HTML は 219 KiB → 553 KiB)。
画面側にもう 1 つ実装を書けばこの増分は消せるが、権限の判定が 2 か所になる。
**食い違うと危ないものを 1 つに保つ**ほうを選んでいる。

## 2 つの層

| | 計算 | API |
|---|---|---|
| 何を運ぶか | 見積もりの数値 | プロジェクト・アカウント・権限 |
| 形式 | `f64` の平坦なバッファ | JSON |
| 呼ぶ頻度 | 1 打鍵ごと | 保存や共有のとき |
| 置き場所 | **常にクライアント** | ローカル or サーバ |
| 仕様 | `docs/ABI.md` | この文書と `docs/openapi.yaml` |

**計算はサーバに置かない。** 計算コアはクライアントに同梱された WASM が
その場で回すので、サーバを建ててもデータの置き場所と権限を見るだけで済む。
オフラインで動く性質も、サーバの負荷が要らない性質も、どちらも保てる。

## 用語: アカウントと人員

「人」が 2 種類出てくる。混ぜると設計が壊れるので、名前で分けている。

| | アカウント (`User`) | 人員 (`Member`) |
|---|---|---|
| 何者か | 操作する主体 | 工数を消化する稼働資源 |
| 持つもの | 権限、システム役割 | 曜日ごとの稼働時間帯、休憩 |
| 例 | ログインする人 | 「鈴木」「外注A」「未割当」 |

1 対 1 とは限らない。ログインしない人員 (外注や「未割当」) もいるし、
見るだけで稼働しないアカウント (マネージャ) もいる。
`Member.userId` で紐づけられるが、任意。

## 権限

### システム役割

| 役割 | できること |
|---|---|
| `admin` | すべてのプロジェクトとアカウントを管理できる |
| `member` | 自分に共有されたプロジェクトだけを扱える |

### プロジェクトごとの役割

強い順に `owner` > `editor` > `viewer`。

| 操作 | viewer | editor | owner |
|---|:-:|:-:|:-:|
| 見る | ○ | ○ | ○ |
| 内容を書き換える | – | ○ | ○ |
| 改名・削除・権限の付け替え | – | – | ○ |
| 複製する (自分のものとして) | ○ | ○ | ○ |

判定は `mhc_api::permission::Actor::may` の 1 か所だけにある。

### 崩してはいけない条件

- **プロジェクトには所有者が必ず 1 人以上いる。** 最後の所有者を降格・
  削除しようとすると `409 Conflict` で止まる。
- **システム管理者が 1 人もいなくならない。** 同じく `409`。
- **唯一の所有者であるアカウントは削除できない。** 先に所有者を移す。
  所有者のいないプロジェクトが生まれないようにするため。
- アカウントを消すと、そのアカウントの権限も一緒に消える (宙に浮かせない)。

## HTTP への写し方

ルートは `mhc_api::protocol::Request::route` が唯一の正で、
`docs/openapi.yaml` はそれを人間向けに書き下したもの。
**両者が一致することはテストで確かめている** (`crates/api/tests/openapi.rs`)。

| 操作 | メソッド | パス |
|---|---|---|
| 自分の情報 | GET | `/v1/me` |
| アカウント一覧 | GET | `/v1/users` |
| アカウントを作る | POST | `/v1/users` |
| アカウントを更新 | PATCH | `/v1/users/{userId}` |
| アカウントを削除 | DELETE | `/v1/users/{userId}` |
| プロジェクト一覧 | GET | `/v1/projects` |
| プロジェクトを作る | POST | `/v1/projects` |
| プロジェクトを取得 | GET | `/v1/projects/{projectId}` |
| 内容を保存 | PUT | `/v1/projects/{projectId}/document` |
| 改名 | PATCH | `/v1/projects/{projectId}` |
| 削除 | DELETE | `/v1/projects/{projectId}` |
| 複製 | POST | `/v1/projects/{projectId}/duplicate` |
| 権限一覧 | GET | `/v1/projects/{projectId}/access` |
| 権限を与える | PUT | `/v1/projects/{projectId}/access/{userId}` |
| 権限を取り消す | DELETE | `/v1/projects/{projectId}/access/{userId}` |

失敗はすべて `{"code": ..., "message": ...}` で返る。

| `code` | HTTP | 意味 |
|---|---:|---|
| `unauthorized` | 401 | 呼び出し元が特定できない |
| `forbidden` | 403 | 権限が足りない |
| `notFound` | 404 | 対象が無い |
| `conflict` | 409 | 状態が合わない (最後の所有者を外そうとした、など) |
| `invalid` | 422 | 入力が不正 |

## ローカルでの動き

`LocalApiClient` は HTTP の代わりに WASM を呼ぶ。

```text
{"actor": "...", "now": "...", "request": {"op": "listProjects"}}
  → api_call → mhc_api::protocol::dispatch → Service
  → {"ok": "true", "reply": {"kind": "projects", "value": [...]}}
```

`actor` と `now` を外から渡しているのは、WASM のなかに**認証も時計も
持ち込まない**ため。サーバで動かすときは、セッションとサーバ時刻がこれを埋める。
おかげで `Service` は同じ入力なら必ず同じ結果を返し、テストしやすい。

ワークスペース (アカウント・プロジェクト・権限) は WASM のなかに丸ごとあり、
変更のたびに JSON で取り出してブラウザに保存する。サーバで言えば
データベースにあたるものを、そのままブラウザに置いている形。

ローカルにはログインが無いので、画面には「別のアカウントとして操作」という
切り替えを置いてある。権限の効き方をその場で確かめられる。

## サーバを建てるとき

残っているのは**薄い層 1 枚**だけ。

1. `Store` を実装する (PostgreSQL や SQLite)。今は `MemoryStore` だけがある。
2. HTTP のハンドラを書く。受け取ったルートとボディを `Request` に組み立て、
   `dispatch` に渡して、`Outcome` を HTTP の応答に写すだけ。
   ステータスは `ApiError::http_status()` が持っている。
3. 認証を足して、セッションから `actor` を埋める。

権限の判定も不変条件もすでに `crates/api` にあるので、ここで書き足すものは無い。
画面側は `HttpApiClient` に接続先を渡すだけで切り替わる。
