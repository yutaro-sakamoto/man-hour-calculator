# 検証のしかた

このアプリが「守る」と言っていることを並べ、**それぞれを何で確かめているか**を
対応させた表。テストが増えても、ここを見れば「どの約束が、どの強さで守られて
いるか」が分かる。

| 段 | 手段 | 分かること |
|---|---|---|
| 1 | 単体テスト | **試した入力について**正しい |
| 2 | 性質テスト (乱択) | 撃った標本の範囲で性質が崩れていない |
| 3 | 差分テスト (相互オラクル) | 独立した 2 実装が食い違っていない |
| 4 | 型による構成 | その値が存在する時点で成り立つ |
| 5 | **有界モデル検査 (Kani)** | 区間の**すべての入力**について成り立つ |
| 6 | **モデル検査 (TLC / TLA+)** | 設計として、**到達しうるすべての状態**で成り立つ |
| — | miri | FFI 層に未定義動作が無い |
| — | ミューテーションテスト | 上の検査に**歯が立っている** |

---

## <<<計算コア>>>

| 約束 | 手段 | どこ |
|---|---|---|
| <<<不変条件>>> | 型 + **Kani** | `<<<file.rs>>>` `<<<harness_name>>>` |
| <<<妥当な入力は必ず受け付ける>>> | **Kani** | `<<<harness_name>>>` |
| <<<性質>>> | 性質テスト | `<<<file.rs>>>` `<<<test_name>>>` |
| <<<2 実装が一致>>> | 差分テスト | `<<<file.rs>>>` |

## <<<権限>>>

| 約束 | 手段 | どこ |
|---|---|---|
| <<<順序の公理>>> | **Kani** | `<<<harness>>>` |
| **<<<順序で破れる不変条件>>>** | **TLC / TLA+** + 単体 | `spec/<<<X>>>.tla` |

### TLC が見つけた筋

反例はそのまま回帰テストに写してある。

1. <<<...>>>

## 保存先 (`Store` の実装すべて)

検査は `<<<conformance.rs>>>` に **1 組だけ**置き、同じものをすべての実装に
当てる。新しい保存先を足すときは、**書く前にこれを通す**。

| 約束 | 手段 | どこ |
|---|---|---|
| <<<...>>> | 適合テスト | `<<<test_name>>>` |

## 配布物

| 約束 | 手段 | どこ |
|---|---|---|
| 1 枚の HTML で、外部通信が 0 件 | E2E (`file://`) | `e2e/tests/app.spec.js` |

---

## 回し方

```sh
cargo test --workspace                     # 1〜3 段 (適合テストも含む)
cargo kani --workspace                     # 5 段
cargo +nightly miri test -p <<<ffi>>>      # FFI
./spec/check.sh                            # 6 段 (TLC)
cargo mutants -p <<<core>>>                # 上の検査に歯があるか
./scripts/check-sync.sh                    # この表と実態がずれていないか
```

最後の 1 つは**この文書そのものを見張る**もの。ハーネスを足して表に書き忘れる、
版が複数箇所で食い違う、リンクの先が消える、といった「読めば分かる食い違い」を
機械で潰す。

## まだ届いていないところ

**モデル化していないもの・検証していないものを、ここに明示的に書く。**
書いていないと「全部検証済み」に読まれる。過去に、仕様がモデル化していない
対象があったせいで穴がすり抜けた。

- <<<...>>>
