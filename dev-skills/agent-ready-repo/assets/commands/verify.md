この変更で壊れていないかを、通しで確かめる。

```sh
<<<fmt / lint / test / build / e2e / check-sync>>>
```

出力を `grep` で絞らず `tail` で見ること。警告行を見落として、失敗を成功と
読み違えたことがある。

$ARGUMENTS に `formal` が含まれるときは、形式手法の層も回す (数分かかる)。

```sh
<<<kani / miri / TLC>>>
```

落ちたものがあれば、**何が落ちたかを出力ごと**報告する。
「たぶんこれが原因」で済ませず、実際に確かめてから直す。
